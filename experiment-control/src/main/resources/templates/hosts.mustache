############### THIS FILE WAS AUTOMATICALLY GENERATED BY TBMGMT

{{#nodes}}
    {{#interfaces}}
        {{#ipv4Address}}
            {{#controlledOverThisConnection}}
{{ipv4Address.hostAddress}} {{node.name}} {{node.name}}.home {{node.name}}-{{name}} {{node.name}}-{{name}}.home
            {{/controlledOverThisConnection}}
            {{^controlledOverThisConnection}}
{{ipv4Address.hostAddress}} {{node.name}}-{{name}} {{node.name}}-{{name}}.home
            {{/controlledOverThisConnection}}
        {{/ipv4Address}}
        {{#ipv6Address}}
            {{#controlledOverThisConnection}}
{{ipv6Address.hostAddress}} {{node.name}} {{node.name}}.home {{node.name}}-{{name}} {{node.name}}-{{name}}.home
            {{/controlledOverThisConnection}}
            {{^controlledOverThisConnection}}
{{ipv6Address.hostAddress}} {{node.name}}-{{name}} {{node.name}}-{{name}}.home
            {{/controlledOverThisConnection}}
        {{/ipv6Address}}
    {{/interfaces}}
{{/nodes}}
